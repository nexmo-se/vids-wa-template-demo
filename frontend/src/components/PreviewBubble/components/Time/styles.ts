import { makeStyles } from "@material-ui/core";
export default makeStyles(() => ({
  time: {
    bottom: 3,
    color: "rgba(0, 0, 0, .4)",
    fontSize: 11,
    height: 15,
    lineHeight: "15px",
    position: "absolute",
    right: 7
  }
}), { index: 1 })